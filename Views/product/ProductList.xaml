<Window x:Class="FreePOS.Views.product.ProductList"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Products" WindowState="Maximized" Height="Auto" Width="Auto" 
        >
    <Grid>
        <Label Content="Search" HorizontalAlignment="Left" Margin="11,12,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="query_tb" TabIndex="1" Text="" HorizontalAlignment="Left" Height="25" Margin="64,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="228"/>
        <ComboBox x:Name="searchType_cb" HorizontalAlignment="Left" Margin="326,4,0,0" VerticalAlignment="Top" Width="145" Height="33">
            <ComboBoxItem Selector.IsSelected="True" Content="Product" Tag="Product"/>
            <ComboBoxItem Content="Category" Tag="Category"/>
            <ComboBoxItem Content="All" Tag="All"/>
        </ComboBox>
        <Button Click="Btn_Search_Transactions_Click" Content="Search" HorizontalAlignment="Left" Margin="554,16,0,0" VerticalAlignment="Top" Width="75"/>

        <DataGrid
            x:Name="dg"
            Margin="0,60,0,0"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            RowEditEnding="dg_roweditended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding id}" />
                <DataGridTextColumn Header="Name" Binding="{Binding name}"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding barcode}"/>
                <DataGridTextColumn Header="Category" Binding="{Binding category}"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding quantity}"/>
                <DataGridTextColumn Header="Sale Active" Binding="{Binding saleactive}"/>
                <DataGridTextColumn Header="Purchase Active" Binding="{Binding purchaseactive}"/>
                <DataGridTextColumn Header="Purchase Price" Binding="{Binding purchaseprice}"/>
                <DataGridTextColumn Header="Carrycost" Binding="{Binding carrycost}"/>
                <DataGridTextColumn Header="Sale Price" Binding="{Binding saleprice}"/>
                <DataGridTextColumn Header="Discount" Binding="{Binding discount}"/>
                <DataGridTemplateColumn Header="Edit">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="edit">Edit</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Report">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="inventorylog">Inventory Log</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
